version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - IMEI_CHECK_API_KEY=${IMEI_CHECK_API_KEY}
      - ALLOWED_USERS=${ALLOWED_USERS}
      - API_TOKEN=${API_TOKEN}